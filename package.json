{
  "dependencies": {
    "mongoose": "^8.8.1",
    "react-icons": "^5.3.0"
  },
  "scripts": {
    "build": "vite build",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../admin && npm install && npm run build"
  }
}
